# 3.1.repair.test.defects — defect inventory

## summary

8 defects found and fixed. test suite now passes.

---

## defect 1: absent return type on base64UrlDecode

**what**: biome lint error `noImplicitFunctionReturnType`
**how**: biome.jsonc added (new file in diff) with `noImplicitFunctionReturnType` rule
**why**: origin/main had no biome.jsonc; declapract added it with stricter lint rules
**fix**: added `: string` return type annotation

```diff
-export const base64UrlDecode = (base64Url: string) =>
+export const base64UrlDecode = (base64Url: string): string =>
```

---

## defect 2: absent return type on isSameSite

**what**: biome lint error `noImplicitFunctionReturnType`
**how**: biome.jsonc added (new file in diff) with `noImplicitFunctionReturnType` rule
**why**: origin/main had no biome.jsonc; declapract added it with stricter lint rules
**fix**: added `: boolean` return type annotation

```diff
-export const isSameSite = (uriA: string, uriB: string) => {
+export const isSameSite = (uriA: string, uriB: string): boolean => {
```

---

## defect 3: absent return type on verifyTokenTimestamps

**what**: biome lint error `noImplicitFunctionReturnType`
**how**: biome.jsonc added (new file in diff) with `noImplicitFunctionReturnType` rule
**why**: origin/main had no biome.jsonc; declapract added it with stricter lint rules
**fix**: added `: void` return type annotation

```diff
-export const verifyTokenTimestamps = ({ token }: { token: string }) => {
+export const verifyTokenTimestamps = ({ token }: { token: string }): void => {
```

---

## defect 4: absent return types in discoverJwksUriFromAuthServerMetadata

**what**: biome lint error `noImplicitFunctionReturnType` on 3 functions
**how**: biome.jsonc added (new file in diff) with `noImplicitFunctionReturnType` rule
**why**: origin/main had no biome.jsonc; declapract added it with stricter lint rules
**fix**: added return type annotations to all three functions

```diff
-export const getOrThrowStandardError = async (uri: string) => {
+export const getOrThrowStandardError = async (uri: string): Promise<AxiosResponse> => {

-export const jwkToPemOrThrowStandardError = (jwk: jwkToPem.JWK) => {
+export const jwkToPemOrThrowStandardError = (jwk: jwkToPem.JWK): string => {

-export const discoverJwksUriFromAuthServerMetadata = async (...) => {
+export const discoverJwksUriFromAuthServerMetadata = async (...): Promise<string> => {
```

---

## defect 5: type re-export without `export type`

**what**: typescript `isolatedModules` error on type re-exports
**how**: tsconfig.json changed to extend `@tsconfig/strictest` which has `isolatedModules: true`
**why**: `export { Type }` is ambiguous when `isolatedModules` is enabled
**fix**: changed to `export type { Type }` syntax

```diff
-export { MinimalTokenClaims } from './getUnauthedClaims';
-export { MinimalTokenHeaderClaims } from './getUnauthedHeaderClaims';
+export type { MinimalTokenClaims } from './getUnauthedClaims';
+export type { MinimalTokenHeaderClaims } from './getUnauthedHeaderClaims';
```

---

## defect 6-7: exports from test files

**what**: biome lint error `noExportsInTest` on test files
**how**: biome.jsonc added (new file in diff) with `noExportsInTest` rule
**why**: origin/main had no biome.jsonc; declapract added it to prevent production imports
**fix**: moved shared test assets to `src/__test_assets__/exampleKeyPairs.ts`

files affected:
- `src/signingAlgorithm/createVerifiableSignature.test.ts`
- `src/signingAlgorithm/isSignatureVerified.test.ts`

created new file:
- `src/__test_assets__/exampleKeyPairs.ts`

---

## defect 8: dpdm cycle detection in node_modules

**what**: `npm run test:lint:cycles` found cycles in node_modules
**how**: diff shows `--exclude '^node_modules'` present in origin/main (pre-existent issue)
**why**: pattern syntax issue — `'^node_modules'` is regex, not glob; dpdm expects glob
**fix**: changed exclude to `'node_modules'` without regex anchor

```diff
-"test:lint:cycles": "dpdm --no-warning --no-tree --exit-code circular:1 --exclude '^node_modules' 'src/**/*.ts'"
+"test:lint:cycles": "dpdm --no-warning --no-tree --exit-code circular:1 --exclude 'node_modules' 'src/**/*.ts'"
```

---

## verification

```sh
THOROUGH=true npm run test
```

result: all tests pass
- 94 unit tests
- 13 integration tests
- 0 acceptance tests (none defined)
