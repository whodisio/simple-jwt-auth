# 3.2.reflect.test.defects — defect classification

## summary

8 defects classified for infrastructure feedback.

---

## defect 1-4: absent return types

**classification**: repo defect (upgraded)

**reason**: this repo's code lacked explicit return types. the upgrade exposed these via `noImplicitFunctionReturnType` rule.

**feedback**: no action needed for infrastructure. repo code was fixed to comply.

---

## defect 5: type re-export without `export type`

**classification**: repo defect (upgraded)

**reason**: this repo used `export { Type }` instead of `export type { Type }`. the upgrade to `@tsconfig/strictest` (with `isolatedModules: true`) exposed this.

**feedback**: no action needed for infrastructure. repo code was fixed to comply.

---

## defects 6-7: exports from test files

**classification**: repo defect (upgraded)

**reason**: this repo exported from test files. the upgrade exposed this via `noExportsInTest` rule.

**feedback**: no action needed for infrastructure. repo code was fixed to comply.

---

## defect 8: circular dependency in transitive deps

**classification**: repo defect (upgraded)

**reason**: the cycle detection command was updated to match declapract best practice (`--exclude '^$'` instead of `--exclude '^node_modules'`). this exposed a circular dependency in helpful-errors@1.5.3 via transitive deps from domain-objects@0.31.9 and test-fns@1.15.7.

**fix**: upgraded domain-objects (0.31.9→0.31.12) and test-fns (1.15.7→1.15.8) to eliminate the cycle.

**feedback**: no action needed for infrastructure. dependencies were upgraded to comply.

---

## summary by classification

| classification | count | defects |
|----------------|-------|---------|
| repo defect (upgraded) | 8 | 1-8 |
| practice bug | 0 | none |

---

## conclusion

all 8 defects were repo defects exposed by the upgrade:
- code that didn't meet stricter standards
- fixed to comply with declapract practices

no practice bugs were found. declapract practices are correct.
